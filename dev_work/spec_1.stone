namespace foo

struct barParam
    name1 String

struct barRes
    name2 String

struct barErr
    err String

route person (barParam, barRes, barErr)
    attrs
        auth = "user"
        allow_app_folder_app = false